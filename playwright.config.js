module.exports = {
  use: {
    // Configure the browser to use (chromium, firefox, or webkit)
    browserName: "chromium",

    // Set the viewport size
    viewport: {
      width: 1280,
      height: 1080,
    },

    // Set the default navigation timeout
    navigationTimeout: 5000,

    // Other options...
  },
};
